DATABASE_URL="file:./dev.db"

# Optional. Without this, /api/analyze-event's LLM call is stubbed out and the
# endpoint returns { abstain: true, ... } with a reason — validation, span
# containment, and resolution logic all still run.
ANTHROPIC_API_KEY=
